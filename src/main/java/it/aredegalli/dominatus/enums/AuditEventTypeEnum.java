package it.aredegalli.dominatus.enums;

public enum AuditEventTypeEnum {
    LOGIN,
    LOGIN_FAIL,
    TOKEN_REFRESH,
    TOKEN_REFRESH_FAIL,
    CHANGE_PASSWORD_REQUEST,
    CHANGE_PASSWORD_FAIL,
    CHANGE_PASSWORD_SUCCESS,
    REGISTER,
    AUDIT_ERROR
}
